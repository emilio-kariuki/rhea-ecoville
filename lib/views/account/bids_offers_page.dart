import 'package:ecoville/utilities/packages.dart';

class BidsAndOffersPage extends StatelessWidget {
  const BidsAndOffersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}