import 'package:ecoville/utilities/packages.dart';

class SellersItems extends StatelessWidget {
  const SellersItems({super.key});
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Sellers Items"),
      ),
      body: const Center(
        child: Text("Sellers Items"),
      ),
    );
  }
}